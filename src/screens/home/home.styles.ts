import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  logo: {
    marginTop: 40,
    height: 200,
    width: 539,
    maxWidth: "60%",
    resizeMode: "contain",
  },
  buttons: {
    marginTop: 60,
    width: "80%",
  },
  btn: {
    marginBottom: 20,
  },
});

export default styles;
