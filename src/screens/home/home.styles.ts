import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  logo: {
    paddingTop: 60,
    height: 200,
    width: 200,
    maxWidth: "60%",
    resizeMode: "contain",
  },
  buttons: {
    marginTop: 60,
  },
  btn: {
    marginBottom: 20,
  },
});

export default styles;
